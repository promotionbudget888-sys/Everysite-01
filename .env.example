# คัดลอกไฟล์นี้เป็น .env แล้วเติมค่าจริงจาก Supabase Dashboard
# (Project Settings → API)  โปรเจกต์: vjlirdirqrzuzzljaixt

# ── Frontend (Vite) — ปลอดภัยที่จะอยู่ฝั่ง client ──────────────────
VITE_SUPABASE_URL=https://vjlirdirqrzuzzljaixt.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# ── ใช้เฉพาะสคริปต์ import (ห้าม commit / ห้ามใส่ในโค้ด frontend) ──
# service_role key มีสิทธิ์เต็ม ข้าม RLS — เก็บเป็นความลับเท่านั้น
SUPABASE_SERVICE_ROLE_KEY=
